# Port the server listens on (default: 3000)
PORT=3000

# Optional: enable HTTPS with a locally-trusted cert (recommended for local dev).
# Generate with: mkcert -install && mkcert localhost
# SSL_CERT=./localhost.pem
# SSL_KEY=./localhost-key.pem
